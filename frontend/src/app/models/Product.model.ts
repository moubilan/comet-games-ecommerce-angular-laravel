import { identifierName } from "@angular/compiler";

export class Product{
    name !: String;
    description !: Text;
    console !: string;
    tag !: string;
    price !: number;
    picture !: string;
}